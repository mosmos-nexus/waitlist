Pill toggle for binary settings — calm, gentle motion.

```jsx
<Switch label="다크 모드" defaultChecked onChange={(v) => setDark(v)} />
<Switch size="sm" checked={on} onChange={setOn} />
```

Controlled (`checked` + `onChange`) or uncontrolled (`defaultChecked`). Sizes `sm`/`md`. On = Core Blue track.
