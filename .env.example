# Notion — used as the Mosmos waitlist database (Name: Title, Email: Email)
NOTION_SECRET=""
NOTION_DB=""

# Resend — sends the Mosmos welcome email (verify mosmos.world in Resend)
RESEND_API_KEY=""

# Upstash Redis — rate limits waitlist signups
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# Public site URL — used for absolute asset URLs in emails
NEXT_PUBLIC_SITE_URL="https://mosmos.world"
